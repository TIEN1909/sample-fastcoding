{
  "name": "axit",
  "version": "1.0.0",
  "scripts": {
    "dev": "parcel ./src/index.html",
    "build": "parcel build ./src/index.html --no-cache --no-source-maps --dist-dir dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tien Vo Thanh",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.10.2"
  }
}
